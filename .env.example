# Example environment variables for Render deployment
# Copy this to .env for local development

# Database Configuration
# For development, leave empty to use SQLite
# For production, Render will provide DATABASE_URL automatically
# DATABASE_URL=postgresql://username:password@hostname:port/database

# JWT Secret
# For development, you can use a simple secret
# For production, use a strong, random secret (Render can generate this)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=8080
HOST=0.0.0.0
