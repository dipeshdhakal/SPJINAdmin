<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>#(title) - SPJIN Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</head>
<body class="bg-gray-100">
    <!-- Navigation -->
    <nav class="bg-blue-900 text-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/admin" class="text-xl font-bold">SPJIN Admin</a>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/admin" class="px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-800 #if(page == "dashboard"):bg-blue-800#endif">Dashboard</a>
                    <a href="/admin/books" class="px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-800 #if(page == "books"):bg-blue-800#endif">Books</a>
                    <a href="/admin/prakarans" class="px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-800 #if(page == "prakarans"):bg-blue-800#endif">Prakarans</a>
                    <a href="/admin/chaupais" class="px-3 py-2 rounded-md text-sm font-medium hover:bg-blue-800 #if(page == "chaupais"):bg-blue-800#endif">Chaupais</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto py-6 px-4">
        #import("content")
    </main>

    <script>
        // Auto-hide alerts after 5 seconds
        setTimeout(function() {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                alert.style.display = 'none';
            });
        }, 5000);
    </script>
</body>
</html>
